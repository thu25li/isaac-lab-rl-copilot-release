"""Isaac Lab RL Engineering Co-pilot — core scripts.

Modules:
    reward_synthesizer: NL task description -> executable reward code
    reward_validator: static + runtime validation of reward code
    reward_library: programmatic access to reward pattern database
    log_analyzer: tensorboard log parsing + anomaly detection
    diagnosis_engine: symptom -> cause -> fix rule-based reasoning
    config_validator: Isaac Lab env config static checks
"""
